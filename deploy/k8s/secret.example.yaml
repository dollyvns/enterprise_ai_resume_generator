# Example only. Do NOT commit real secrets.
# In production use External Secrets / CSI Secrets Store / cloud secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: resume-api-secrets
type: Opaque
stringData:
  JWT_SECRET: "replace-via-secret-manager"
  APP_USER_USERNAME: "replace-or-use-enterprise-idp"
  APP_USER_PASSWORD_HASH: "replace-via-secret-manager"
  OPENAI_API_KEY: "replace-via-secret-manager"
